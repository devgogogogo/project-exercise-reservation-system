spring:
  jpa:
    hibernate:
      ddl-auto: create-drop   # ✅ 테스트 시작 시 엔티티 기반으로 테이블 자동 생성, 끝나면 삭제
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.springframework: WARN
    com.fastcampus: DEBUG