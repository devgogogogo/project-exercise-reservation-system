spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop     # 필요에 따라 update/validate로 변경 가능
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false

  data:
    redis:

logging:
  level:
    org.springframework: WARN
    com.fastcampus: DEBUG

jwt:
  # 샘플 키 — 실제로는 환경마다 바꿔 써라
  secret-key: Y3Jhc2gtc2VydmljZS1zdXBlci1zZXZyZXQta2V5203874yosdhgf
